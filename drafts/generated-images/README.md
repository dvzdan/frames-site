# Generated Image Staging

Use this folder for generated or experimental image attempts.

- Put new candidate images in `candidates/`.
- Put rejected or failed attempts in `failed/`.
- Move only approved final images into `assets/` or `assets/setup-kit/`.

Live asset folders should contain only files currently used by the website or
manual.
